# HTML 웹페이지 생성 조건 (공통본)

## 1. 목적

`ax_paradigm.html`, `ax_framework.html`, `vibe_coding.html`의 **공통 디자인/기능**만 표준으로 정의한다.  
신규 콘텐츠 HTML은 본 문서를 기본으로 제작한다.

---

## 2. 구현 기본

- 단일 HTML 파일(HTML/CSS/Vanilla JS)로 독립 실행 가능해야 한다.
- 외부 라이브러리 의존 없이 로컬 파일 실행 및 정적 배포가 가능해야 한다.
- 15인치 노트북(예: 1440x900, 1920x1080)에서 우선 최적화한다.

---

## 3. 레이아웃 공통 구조

- 기본은 **좌측 사이드바 + 우측 콘텐츠 스테이지** 2열 구조.
- 사이드바는 데스크톱에서 고정(`position: fixed` 또는 동등 동작), 내부 목록은 스크롤 가능.
- 메인 영역은 사이드바 폭만큼 좌측 여백을 확보한다.
- 모바일(약 900px 이하)에서는 상단 내비게이션형/단일열 구조로 전환한다.

---

## 4. 사이드바 디자인 표준

### 4.1 컬러

- 사이드바 기본 배경: `#0f172a`
- 기본 텍스트: 흰색 계열
- 비활성 메뉴 텍스트: 흰색 반투명 계열
- 호버/활성 상태: 배경 강조 + 흰색 텍스트

### 4.2 필수 구성요소

- 문서/세션 제목(브랜드 또는 강의명)
- 현재 페이지 진행 상태(현재/전체, 진행바)
- Agenda/목차 목록
- 사이드바 하단 고정 성격의 홈 이동 링크

### 4.3 하단 링크 규칙

- 사이드바 맨 아래에 `"목록으로"` 링크를 둔다.
- 링크는 홈(`/`)으로 이동하며 `target="_top"`을 사용한다.

---

## 5. 카드/슬라이드 공통 규칙

- 각 페이지는 카드(또는 슬라이드) 단위로 구성한다.
- 카드 내부는 `헤더 / 본문 / 하단 안내`의 3영역 구조를 기본으로 한다.
- 본문 영역은 콘텐츠가 길 경우 카드 내부 스크롤을 허용한다.
- 카드 클릭 시 다음 카드로 이동하는 동작을 제공한다.
- 마지막 카드는 다음 이동 대상이 없으므로 이동 동작을 막는다.

---

## 6. 타이포그래피/가독성

- 한국어 본문은 `word-break: keep-all` 기반으로 줄바꿈 품질을 유지한다.
- 제목/본문/보조 문구는 화면 크기에 따라 `clamp()` 등 반응형 크기를 적용한다.
- 목록/문단/표는 좌측 정렬을 기본으로 한다.

---

## 7. 강조(콜아웃) 스타일 공통

- 강조 박스/핵심 문구/인용 박스는 연두 계열을 공통 사용:
  - 강조선: `#65a30d`
  - 강조 배경: `#f7fee7`
  - 강조 텍스트: 어두운 녹색 또는 기본 본문 대비색
- 강조 스타일은 정보 중요도 표현 용도로만 사용하고, 기본 본문 색상 체계와 혼용하지 않는다.

---

## 8. 상호작용(내비게이션) 공통

- 현재 카드에 맞춰 Agenda 활성 상태를 동기화한다.
- 진행률(현재 번호/전체 개수, 진행바)을 실제 카드 수 기준으로 계산한다.
- 키보드 이동(예: `ArrowUp/ArrowDown`, `PageUp/PageDown`, `Home/End`)을 지원한다.
- 링크/버튼/입력요소 클릭 시 카드 이동 이벤트가 오작동하지 않도록 예외 처리한다.
- 텍스트 선택 중에는 카드 이동이 트리거되지 않도록 처리한다.

---

## 9. 반응형/접근성 공통

- 900px 이하에서 사이드바-메인 2열을 단일열로 안전하게 전환한다.
- 카드 내 다열 그리드는 태블릿/모바일에서 1열 또는 2열로 축소한다.
- 키보드 포커스 가능한 카드 요소는 `aria-label` 또는 동등한 접근성 정보를 제공한다.

---

## 10. 검수 체크리스트 (공통)

- [ ] 사이드바 배경이 `#0f172a`로 적용되었는가?
- [ ] 사이드바 하단에 `"목록으로"` 링크가 있고 홈(`/`)으로 이동하는가?
- [ ] 카드/슬라이드 개수와 진행률/Agenda 동기화가 일치하는가?
- [ ] 카드 클릭/키보드로 다음·이전 이동이 정상 동작하는가?
- [ ] 링크/버튼 클릭, 텍스트 선택 시 오작동 이동이 없는가?
- [ ] 강조 박스가 `#65a30d` / `#f7fee7` 공통 팔레트를 사용하는가?
- [ ] 15인치 노트북 해상도에서 카드가 과도하게 잘리지 않는가?
- [ ] 모바일 폭에서 레이아웃이 단일열로 안정적으로 전환되는가?

---

## 11. 제외 원칙

다음 항목은 **페이지별 특화 요소**이므로 공통 지침에서 제외한다.

- 특정 강의 전용 문구/브랜드명/섹션명
- 특정 페이지에서만 쓰는 컴포넌트(예: 개별 실습 박스 이름, 특정 데이터 표 구조)
- 특정 콘텐츠 시나리오(예: 단일 과정 전용 플로우, 개별 사례 전용 규칙)
- 특정 파일에만 존재하는 색상 체계(공통 다크 사이드바/연두 강조 체계 외)
